# 📦 Releases

Os pacotes `.zip` oficiais da extensão devem ser publicados na aba **Releases** do GitHub.

Para gerar um pacote local para Chrome Web Store, rode:

```bash
./scripts/package-extension.sh
```

O arquivo será criado em `dist/`, pasta ignorada pelo Git.
